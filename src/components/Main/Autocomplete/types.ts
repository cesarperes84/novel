export interface AutocompleteProps {
  getOptionLabel: any;
  noOptionsText: string;
  inputValue: string;
  onInputChange: any;
  label: string;
  options: any;
  open: boolean;
  onOpen: any;
  onClose: any;
}
